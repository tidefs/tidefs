# TideFS Claim Registry

Maturity: generated claim registry.

This file is generated from `validation/claims.toml` by `cargo run -p tidefs-xtask -- check-claims-gate`. Edit the registry, not this document.

`validate-claim <id>` succeeds only for `validated` claims with fresh evidence artifacts. Planned, blocked, and invalid claims fail closed and remain non-product claims.

| Claim id | Status | Scope | Required evidence | Blockers | Generated text |
|---|---|---|---|---|---|
| `storage.write_fsync.crash_safety.v1` | `planned` | local-filesystem write and fsync durability | model-crash-matrix<br>runtime-crash-oracle<br>claims-gate-review | GitHub issue #286 model-first evidence is bounded to the executable model, not local runtime crash injection.<br>Runtime crash-oracle artifacts must be recorded before present-tense durability wording. | Planned write/fsync crash-safety claim for local filesystem commits. TideFS must keep this as future capability wording until model and runtime crash-oracle evidence exists. |
| `namespace.rename.atomicity.v1` | `planned` | local namespace rename operations | model-crash-matrix<br>runtime-namespace-crash-artifact<br>claims-gate-review | GitHub issue #286 model-first evidence is bounded to the executable model, not local runtime crash injection.<br>Runtime evidence for interrupted rename sequences is not recorded (#596).<br>Rename directory-entry and orphan-index no-hidden-queue source/registry review receipt recorded (#597); active rename/orphan-index runtime admission proof remains pending (#720). | Planned rename atomicity claim for local namespace operations. TideFS must keep rename wording aspirational until model and runtime crash evidence covers the claimed scope. |
| `local.vfs.write_fsync_crash.v1` | `validated` | local VFS write and fsync crash recovery | model-crash-matrix<br>runtime-crash-oracle<br>claims-gate-review | - | Validated bounded local VFS write/fsync crash claim. TideFS has model-crash-matrix, OpFsyncBeforeFlush runtime-crash-oracle, and claims-gate review evidence for the local write -> fsync -> read -> crash/recover path. This is not production crash-safety proof beyond the local VFS hook scope. |
| `local.vfs.rename_atomic_crash.v1` | `blocked` | local VFS rename crash recovery | model-crash-matrix<br>runtime-namespace-crash-artifact<br>claims-gate-review | Model-only issue #286 evidence is not local runtime crash evidence.<br>Local runtime rename crash injection remains blocked until #596 records the runtime crash artifact.<br>Rename directory-entry and orphan-index no-hidden-queue source/registry review receipt recorded (#597); active rename/orphan-index runtime admission proof remains pending (#720). | Blocked local VFS rename crash claim. TideFS must keep rename crash-safety wording behind model and runtime evidence. |
| `scheduler.dirty_debt.no_hidden.v1` | `blocked` | foreground and background dirty-work accounting | admission-budget-model<br>queue-depth-runtime-artifact<br>claims-gate-review | GitHub issue #287 owns performance-contract work classes and no-hidden-queue gates.<br>No runtime artifact proves dirty work cannot hide outside budgeted queues. | Blocked no-hidden-dirty-debt claim for foreground and background work accounting. TideFS must not claim hidden dirty work is eliminated until budgeted queue evidence exists. |
| `scrub.foreground_read.protected.v1` | `planned` | foreground read protection from scrub and repair work | scrub-read-isolation-model<br>runtime-scrub-read-artifact<br>claims-gate-review | Scrub/read isolation authority is not proven by a focused runtime row.<br>Foreground read latency and correctness artifacts under scrub load are missing. | Planned foreground-read protection claim for scrub and repair work. TideFS must describe this as future proof work until isolation and runtime artifacts cover foreground reads. |
| `perf.local.no_unbounded_dirty_debt.v1` | `validated` | local dirty write admission and budgeted queue accounting | admission-budget-model<br>no-hidden-queue-gate<br>queue-depth-runtime-artifact<br>claims-gate-review | - | Validated bounded local no-unbounded-dirty-debt claim. TideFS has admission-budget model evidence, no-hidden-queue gate evidence, mounted FUSE queue-depth runtime evidence, and claims-gate review evidence for the local smoke-mount quick workload. This does not validate scheduler-wide dirty-debt coverage, scrub/read isolation, crash recovery, or production performance readiness. |
| `perf.local.foreground_read_not_blocked_by_scrub.v1` | `blocked` | local foreground read service while scrub work is pending | scrub-read-isolation-model<br>runtime-scrub-read-artifact<br>claims-gate-review | GitHub issue #287 first slice records a deterministic oracle, not a mounted scrub/read runtime artifact.<br>Scrub runtime queues have not been wired through the typed service curve and admission metadata. | Blocked local foreground-read-not-blocked-by-scrub claim. TideFS must treat the oracle as model evidence only until runtime scrub/read artifacts cover the claimed path. |
| `offload.ready.non_authoritative.v1` | `validated` | non-authoritative offload descriptor, CPU reference backend, and completion validation | cpu-reference-backend<br>descriptor-completion-validation<br>claims-gate-review | - | Validated non-authoritative offload claim for v1 descriptor validation, CPU reference checksum/parity/scrub kernels, buffer-lease matching, and completion validation. This does not validate GPU/FPGA production acceleration, DMA, kernel, RDMA, storage-runtime integration, or hardware equivalence. |
| `ublk.qid_tag.exactly_once_completion.v1` | `planned` | uBLK qid/tag request lifecycle | qid-tag-state-model<br>runtime-ublk-completion-artifact<br>claims-gate-review | The qid/tag state model is bounded pure-model evidence, not uBLK daemon or block-volume runtime evidence.<br>The focused qemu-ublk-smoke runtime artifact is bounded evidence; broader runtime coverage (queue-depth scaling, multi-queue, error injection, and broader operation mix) is still required before product wording can strengthen. | Planned uBLK qid/tag exactly-once completion claim. TideFS has bounded model evidence, a focused runtime completion artifact, and a claims-gate review; broader runtime coverage is still required before product wording can strengthen. |
| `ublk.started_export.live_service_loop.v1` | `validated` | uBLK started export admission and daemon-owned live data-queue service loop | runtime-ublk-started-export-admission-artifact<br>runtime-ublk-completion-artifact<br>claims-gate-review | - | Validated bounded uBLK started-export live service-loop claim. TideFS has started-export-service-loop-model, qid-tag-completion runtime, started-export-admission runtime (qemu-ublk-smoke), and claims-gate review evidence. This is not fio workload breadth, mkfs/mount acceptance, online resize, crash durability, or production block-device readiness. |
| `kernel.teardown.no_work_after.v1` | `blocked` | kernel teardown and post-teardown work exclusion | kernel-context-token-model<br>teardown-race-proof-artifact<br>claims-gate-review | The recorded teardown proof review is bounded source/model evidence, not mounted Linux runtime evidence.<br>T5 mounted-kernel teardown stress with Linux workqueue and callback activity tracing is missing.<br>T6 mounted kernel I/O teardown and recovery rows across the filesystem runtime are missing. | Blocked kernel no-work-after-teardown claim. TideFS has bounded source-model context-token and teardown race review evidence, but mounted Linux T5/T6 runtime evidence is still required before product wording can validate the claim. |
