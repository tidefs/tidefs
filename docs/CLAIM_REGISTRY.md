# TideFS Claim Registry

Maturity: generated claim registry.

This file is generated from `validation/claims.toml` by `cargo run -p tidefs-xtask -- check-claims-gate`. Edit the registry, not this document.

`validate-claim <id>` succeeds only for `validated` claims with fresh evidence artifacts. Planned, blocked, and invalid claims fail closed and remain non-product claims.

| Claim id | Status | Scope | Required evidence | Blockers | Generated text |
|---|---|---|---|---|---|
| `storage.write_fsync.crash_safety.v1` | `planned` | local-filesystem write and fsync durability | model-crash-matrix<br>runtime-crash-oracle<br>claims-gate-review | GitHub issue #286 model-first evidence is bounded to the executable model, not local runtime crash injection.<br>Runtime crash-oracle artifacts must be recorded before present-tense durability wording. | Planned write/fsync crash-safety claim for local filesystem commits. TideFS must keep this as future capability wording until model and runtime crash-oracle evidence exists. |
| `namespace.rename.atomicity.v1` | `planned` | local namespace rename operations | model-crash-matrix<br>runtime-namespace-crash-artifact<br>claims-gate-review | GitHub issue #286 model-first evidence is bounded to the executable model, not local runtime crash injection.<br>Runtime evidence for interrupted rename sequences is not recorded. | Planned rename atomicity claim for local namespace operations. TideFS must keep rename wording aspirational until model and runtime crash evidence covers the claimed scope. |
| `local.vfs.write_fsync_crash.v1` | `blocked` | local VFS write and fsync crash recovery | model-crash-matrix<br>runtime-crash-oracle<br>claims-gate-review | Model-only issue #286 evidence is not local runtime crash evidence.<br>Local runtime crash injection remains blocked until local-filesystem write-set conflicts clear. | Blocked local VFS write/fsync crash claim. TideFS must treat model-only crash matrices as planning evidence, not production runtime durability proof. |
| `local.vfs.rename_atomic_crash.v1` | `blocked` | local VFS rename crash recovery | model-crash-matrix<br>runtime-namespace-crash-artifact<br>claims-gate-review | Model-only issue #286 evidence is not local runtime crash evidence.<br>Local runtime rename crash injection remains blocked until local-filesystem write-set conflicts clear. | Blocked local VFS rename crash claim. TideFS must keep rename crash-safety wording behind model and runtime evidence. |
| `scheduler.dirty_debt.no_hidden.v1` | `blocked` | foreground and background dirty-work accounting | admission-budget-model<br>queue-depth-runtime-artifact<br>claims-gate-review | GitHub issue #287 owns performance-contract work classes and no-hidden-queue gates.<br>No runtime artifact proves dirty work cannot hide outside budgeted queues. | Blocked no-hidden-dirty-debt claim for foreground and background work accounting. TideFS must not claim hidden dirty work is eliminated until budgeted queue evidence exists. |
| `scrub.foreground_read.protected.v1` | `planned` | foreground read protection from scrub and repair work | scrub-read-isolation-model<br>runtime-scrub-read-artifact<br>claims-gate-review | Scrub/read isolation authority is not proven by a focused runtime row.<br>Foreground read latency and correctness artifacts under scrub load are missing. | Planned foreground-read protection claim for scrub and repair work. TideFS must describe this as future proof work until isolation and runtime artifacts cover foreground reads. |
| `offload.ready.non_authoritative.v1` | `planned` | offload acceleration authority | cpu-reference-backend<br>offload-equivalence-artifact<br>claims-gate-review | NEXTGEN-007 non-authoritative offload evidence is not present.<br>CPU/reference authority must remain the only product truth until equivalence artifacts exist. | Planned non-authoritative offload claim. TideFS must keep offload wording as helper-path acceleration until CPU/reference equivalence evidence exists. |
| `ublk.qid_tag.exactly_once_completion.v1` | `planned` | uBLK qid/tag request lifecycle | qid-tag-state-model<br>runtime-ublk-completion-artifact<br>claims-gate-review | GitHub issue #289 owns the uBLK qid/tag lifecycle model.<br>No focused runtime artifact proves exactly-once completion for qid/tag pairs. | Planned uBLK qid/tag exactly-once completion claim. TideFS must keep this as model work until lifecycle and runtime artifacts cover the completion contract. |
| `kernel.teardown.no_work_after.v1` | `planned` | kernel teardown and post-teardown work exclusion | kernel-context-token-model<br>teardown-race-proof-artifact<br>claims-gate-review | GitHub issue #291 owns kernel context tokens and teardown race proof harness work.<br>No artifact proves work cannot run after teardown. | Planned kernel no-work-after-teardown claim. TideFS must keep this as future proof work until context-token and teardown race evidence exists. |
