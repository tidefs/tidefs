artifact_version = 1
evidence_class = "claims-gate-review"
source = "claims-gate"
scope = "model-runtime-boundary-review"
issue = 329

model_artifact = "validation/artifacts/crash-oracle/model-crash-matrices.json"
model_evidence_class = "model-crash-matrix"
model_evidence_scope = "bounded model-only crash matrix; no local runtime crash injection"
runtime_claim_boundary = "model crash matrices remain model-only; local runtime crash claims require matching runtime artifacts before validation"

reviewed_claim_ids = [
  "storage.write_fsync.crash_safety.v1",
  "namespace.rename.atomicity.v1",
  "local.vfs.write_fsync_crash.v1",
  "local.vfs.rename_atomic_crash.v1",
]

missing_runtime_evidence_classes = []
runtime_evidence_status = "local-write-mounted-runtime-present-as-environment-refusal-rename-runtime-present-scope-boundaries-blocked"
decision = "write-fsync-environment-refusal-rename-planned-blocked-fail-closed"

boundary_review = [
  "The registered model-crash-matrix artifact is model-only evidence from tidefs-crash-oracle and does not satisfy runtime crash evidence classes.",
  "The local VFS write/fsync runtime-crash-oracle artifact now records a current-source mounted FUSE environment refusal: its explicit MountHarness command did not reach the runtime row because host disk headroom was below the worker safety floor. It does not satisfy local.vfs.write_fsync_crash.v1.",
  "The local VFS rename runtime-namespace-crash-artifact records the rename -> fsync -> read -> process-exit/recover -> read boundary and satisfies runtime evidence only for the bounded local VFS rename scope.",
  "The storage-wide write/fsync, namespace rename, and local rename crash claims remain planned or blocked and fail-closed until their registered runtime evidence has a pass outcome, and their model evidence, dependency evidence, no-hidden-queue evidence, and claims-gate review are all fresh for the claimed scope.",
]

non_claims = [
  "This review does not validate local runtime crash injection beyond the refused mounted FUSE write row and the bounded ProcessExitAfterRead rename artifact.",
  "This review does not validate the interrupted fsync payload as durable.",
  "This review does not validate rename directory-entry, link/unlink, or orphan-index no-hidden-queue evidence.",
  "This review does not validate production crash safety.",
  "This review does not validate mounted runtime durability, writeback, mmap, page-cache coherency, distributed recovery, or OpenZFS/Ceph-class status.",
]
