[package]
name = "tidefs-types-reclaim-queue-core"
version.workspace = true
edition.workspace = true
publish.workspace = true
license.workspace = true
authors.workspace = true

description = """
Authority type definitions for refcount delta-based incremental data cleanup
queues: ReclaimQueueEntry, ReclaimStats, ReclaimIntegrityError, QueueFamily.
no_std / forbid(unsafe_code).
"""

[dependencies]
serde = { version = "1", default-features = false, features = ["derive"], optional = true }
