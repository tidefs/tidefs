[package]
name = "tidefs-local-filesystem-fuzz"
version = "0.0.0"
edition = "2021"
publish = false
license = "GPL-2.0-only WITH Linux-syscall-note"

[package.metadata]
cargo-fuzz = true

[dependencies]
libfuzzer-sys = "0.4"
arbitrary = { version = "1", features = ["derive"] }

[dependencies.tidefs-local-filesystem]
path = ".."

# Prevent this from being built as part of the workspace's default members
# (cargo-fuzz targets require nightly and are built separately)
[workspace]

[lib]
path = "fake_lib.rs"

[[bin]]
name = "intent_log_entry_decode"
path = "fuzz_targets/intent_log_entry_decode.rs"
test = false
doc = false

[[bin]]
name = "receive_checkpoint_decode"
path = "fuzz_targets/receive_checkpoint_decode.rs"
test = false
doc = false
