[package]
name = "tidefs-types-polymorphic-xattr-core"
version.workspace = true
edition.workspace = true
publish.workspace = true
license.workspace = true
authors.workspace = true

description = """
Authority type definitions for the polymorphic xattr storage scheme with two
canonical representations (XattrBundleV1 inline O(n) vs XattrBtreeRootV1
external B+tree O(log n)), switching thresholds with hysteresis, and
dataset-level DatasetXattrPolicy. no_std / forbid(unsafe_code).
"""

[features]
default = []
alloc = []

[dependencies]
