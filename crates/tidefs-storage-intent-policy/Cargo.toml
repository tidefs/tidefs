[package]
name = "tidefs-storage-intent-policy"
version.workspace = true
edition.workspace = true
publish.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation.workspace = true
authors.workspace = true

description = """
Typed storage-intent policy source compilation for dataset-scoped prefetch and
media residency. Merges pool, dataset, mount, caller, and maintenance sources
into #967 policy envelopes without executing placement or relocation.
"""

[dependencies]
tidefs-storage-intent-core = { path = "../tidefs-storage-intent-core" }

[lib]
path = "src/lib.rs"
