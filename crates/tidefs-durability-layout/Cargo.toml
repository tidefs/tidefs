[package]
name = "tidefs-durability-layout"
version.workspace = true
edition.workspace = true
publish.workspace = true
license.workspace = true
authors.workspace = true

description = "TideFS-native durability layout descriptor unifying mirror and erasure-style policies across device and node failure domains"

[dependencies]
tidefs-binary_schema-core = { path = "../tidefs-binary_schema-core" }
tidefs-binary_schema-checksum = { path = "../tidefs-binary_schema-checksum" }
blake3 = { version = "1", default-features = false }

[dev-dependencies]

[lib]
path = "src/lib.rs"
