[package]
name = "tidefs-relocation-planner"
version.workspace = true
edition.workspace = true
publish.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation.workspace = true
authors.workspace = true

description = "Relocation planner: reclaim/tiering/policy-change/drain relocation flow orchestration — P8-03 data_copy_5"

[dependencies]
tidefs-replication-model = { path = "../tidefs-replication-model" }
tidefs-membership-epoch = { path = "../tidefs-membership-epoch" }
tidefs-locator-table = { path = "../tidefs-locator-table" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
tempfile = "3"
tidefs-local-object-store = { path = "../tidefs-local-object-store" }
tidefs-types-pool-label-core = { path = "../tidefs-types-pool-label-core" }

[lib]
path = "src/lib.rs"
