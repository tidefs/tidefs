[package]
name = "tidefs-storage-intent-media-capability-refresh"
version.workspace = true
edition.workspace = true
publish.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation.workspace = true
authors.workspace = true

description = """
Generation, freshness, and invalidation records for storage-intent media
capability evidence. Consumes tidefs-storage-intent-core capability records and
fails durable, promotion, feedback, and claim-facing roles closed when producer
evidence is stale, contradicted, compacted, or superseded.
"""

[dependencies]
tidefs-storage-intent-core = { path = "../tidefs-storage-intent-core" }

[lib]
path = "src/lib.rs"
