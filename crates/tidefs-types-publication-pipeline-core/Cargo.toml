[package]
name = "tidefs-types-publication-pipeline-core"
version.workspace = true
edition.workspace = true
publish.workspace = true
license.workspace = true
authors.workspace = true

description = "Portable no_std publication_pipeline publication ticket core types for tidefs"

[dependencies]
tidefs-types-control-plane-core = { path = "../tidefs-types-control-plane-core" }

[lib]
path = "src/lib.rs"
