[package]
name = "tidefs-dir-index"
version.workspace = true
edition.workspace = true
publish.workspace = true
license.workspace = true
authors.workspace = true

description = """
Persistent directory index with DirEntry/DirPage binary format, object-store
CRUD, ordered iteration, and hysteresis-driven representation switching.
"""

[dependencies]
tidefs-local-object-store = { path = "../tidefs-local-object-store", optional = true }
tidefs-types-polymorphic-directory-index-core = { path = "../tidefs-types-polymorphic-directory-index-core", features = ["alloc"] }
blake3 = { version = "1", default-features = false }
tidefs-btree = { path = "../tidefs-btree" }
tidefs-kernel-storage-io = { path = "../tidefs-kernel-storage-io", optional = true }
tidefs-types-vfs-core = { path = "../tidefs-types-vfs-core", optional = true }

[dev-dependencies]
tempfile = "3"
proptest = { version = "=1.9.0", default-features = false, features = ["std"] }

[features]
default = ["in-memory-dir-index"]
in-memory-dir-index = []
persistent-dir-index = ["tidefs-local-object-store", "std"]
kernel = ["tidefs-kernel-storage-io", "tidefs-types-vfs-core"]

# Backward compatibility: std enables persistent-dir-index
std = ["persistent-dir-index"]
