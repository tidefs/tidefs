[package]
name = "tidefs-replication-model"
version.workspace = true
edition.workspace = true
publish.workspace = true
license.workspace = true
authors.workspace = true

description = "Deterministic replicated object/root storage model and degraded read/write tests"

[dependencies]
thiserror = "2"
serde = { version = "1", features = ["derive"] }
tidefs-membership-epoch = { path = "../tidefs-membership-epoch" }

[dev-dependencies]
serde_json = "1"

[lib]
path = "src/lib.rs"
