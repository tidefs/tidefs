[package]
name = "tidefs-witness-set"
version.workspace = true
edition.workspace = true
publish.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation.workspace = true
authors.workspace = true

description = "P8-03 witness set mechanism: quorum-based claim verification, witness selection, and receipt integration"

[dependencies]
serde = { version = "1", features = ["derive"] }
ed25519-dalek = { version = "1", features = ["serde"] }
rand = "0.7"
thiserror = "2"
serde_json = "1"
blake3 = "=1.5.5"
tidefs-binary_schema-core = { path = "../tidefs-binary_schema-core" }
tidefs-membership-epoch = { path = "../tidefs-membership-epoch" }

[dev-dependencies]

[lib]
path = "src/lib.rs"
