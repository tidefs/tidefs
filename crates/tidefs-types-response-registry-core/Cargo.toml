[package]
name = "tidefs-types-response-registry-core"
version.workspace = true
edition.workspace = true
publish.workspace = true
license.workspace = true
authors.workspace = true

description = "Portable no_std response_registry response-envelope, index, and recall-binding core types for tidefs"

[dependencies]
tidefs-types-control-plane-core = { path = "../tidefs-types-control-plane-core" }

[lib]
path = "src/lib.rs"
