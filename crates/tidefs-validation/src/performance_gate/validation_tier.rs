//! Validation tier for performance gate rows.
//!
//! Re-exported from the unified validation schema.
//! All crate consumers must use this single canonical `ValidationTier`.

pub use crate::validation_schema::ValidationTier;
