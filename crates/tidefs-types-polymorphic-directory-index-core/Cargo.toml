[package]
name = "tidefs-types-polymorphic-directory-index-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation.workspace = true
authors.workspace = true

description = """
Shared runtime types for the polymorphic directory index with an inline list,
explicitly runtime-only B+tree metadata, switching thresholds with hysteresis,
tagged 64-bit readdir cookies, and dataset-level DirPolicy. no_std /
forbid(unsafe_code).
"""

[features]
default = []
alloc = []

[dependencies]
