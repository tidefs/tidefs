[package]
name = "tidefs-rebalance-planner"
version.workspace = true
edition.workspace = true
publish.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation.workspace = true
authors.workspace = true

description = "Rebalance planner: capacity redistribution orchestration with movement budget, epoch gating, and anti-affinity preservation"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"
tidefs-membership-epoch = { path = "../tidefs-membership-epoch" }
tidefs-replication-model = { path = "../tidefs-replication-model" }
tidefs-rebuild-planner = { path = "../tidefs-rebuild-planner" }
tidefs-placement-planner = { path = "../tidefs-placement-planner" }
tidefs-replica-health = { path = "../tidefs-replica-health" }
tidefs-commit_group = { path = "../tidefs-commit_group" }
tidefs-space-accounting = { path = "../tidefs-space-accounting" }

[lib]
path = "src/lib.rs"
