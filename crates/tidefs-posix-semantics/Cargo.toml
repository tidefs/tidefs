[package]
name = "tidefs-posix-semantics"
version.workspace = true
edition.workspace = true
publish.workspace = true
license.workspace = true
authors.workspace = true

description = """
Deterministic POSIX semantics helpers: sticky-bit, killpriv, setgid inheritance,
relatime policy, and permission evaluation. Pure, no_std / forbid(unsafe_code),
ported from the v0.262 Python reference (posix_semantics.py).
"""

[features]
default = []
alloc = []

[dependencies]

tidefs-types-vfs-core = { path = "../tidefs-types-vfs-core" }
