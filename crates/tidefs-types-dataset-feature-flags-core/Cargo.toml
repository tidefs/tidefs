[package]
name = "tidefs-types-dataset-feature-flags-core"
version.workspace = true
edition.workspace = true
publish.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation.workspace = true
authors.workspace = true

description = """
Authority type definitions for per-dataset feature flags with three
compatibility classes (compat/ro_compat/incompat), reverse-DNS feature
name registry, and canonical V1 feature constants. no_std / forbid(unsafe_code).
"""

[features]
default = []
alloc = []

[dependencies]
