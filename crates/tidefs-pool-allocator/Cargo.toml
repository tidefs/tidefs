[package]
name = "tidefs-pool-allocator"
version.workspace = true
edition.workspace = true
publish.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation.workspace = true
authors.workspace = true

description = """
Pool-level allocator wrapping SegmentFreeMap with per-metaslab allocation cursors,
metaslab selection policy (least-free-first with round-robin tiebreak), ENOSPC
propagation, and space pressure signaling. Policy layer above the deterministic
free-segment tracker (G1 Phase 6 completion).
"""

[dependencies]
tidefs-spacemap-allocator = { path = "../tidefs-spacemap-allocator" }

[dev-dependencies]

[lib]
path = "src/lib.rs"
