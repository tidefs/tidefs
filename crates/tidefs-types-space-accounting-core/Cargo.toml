[package]
name = "tidefs-types-space-accounting-core"
version.workspace = true
edition.workspace = true
publish.workspace = true
license.workspace = true
authors.workspace = true
description = "TideFS space accounting authority type definitions: logical/physical counters, space domains, snapshot deadlist records, and admission control"

[features]
default = []
alloc = []

[dependencies]
serde = { version = "1", default-features = false, features = ["derive"], optional = true }
blake3 = { version = "=1.5.5", default-features = false }
